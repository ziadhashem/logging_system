{% extends 'base.html.twig' %}

{% block title %}Edit Event Log{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Event Log</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('event_log_index') }}">
                    View All Logged Events
                </a>
            </div>
            <div class="card-body">
                {% include 'event_log/_form.html.twig' with {'button_label': 'Update Log', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}
